apiVersion: apps/v1
kind: Deployment
metadata:
  name: monitoring-frontend
  namespace: apps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: monitoring-frontend
  template:
    metadata:
      labels:
        app: monitoring-frontend
    spec:
      volumes:
      - name: nginx-logs
        emptyDir: {}
      - name: fluent-bit-config
        configMap:
          name: fluent-bit-frontend-config
      - name: fluent-bit-etc
        emptyDir: {}
      containers:
      - name: frontend
        image: monitoring-frontend:latest   # override: kustomize edit set image monitoring-frontend:latest=YOUR_REGISTRY/monitoring-frontend:latest
        imagePullPolicy: Always   # always pull so new builds (namespace selector, etc.) are served
        ports:
        - containerPort: 80
        volumeMounts:
        - name: nginx-logs
          mountPath: /var/log/nginx
        resources:
          requests:
            memory: "32Mi"
            cpu: "20m"
          limits:
            memory: "64Mi"
            cpu: "100m"
      - name: fluent-bit
        image: fluent/fluent-bit:2.2
        command: ["/bin/sh", "/config/entrypoint.sh"]
        # Optional: set KAFKA_BOOTSTRAP_SERVERS (e.g. cluster-kafka-bootstrap.kafka.svc.cluster.local:9092) to ship logs to Kafka
        volumeMounts:
        - name: nginx-logs
          mountPath: /var/log/nginx
          readOnly: true
        - name: fluent-bit-config
          mountPath: /config
          readOnly: true
        - name: fluent-bit-etc
          mountPath: /fluent-bit/etc
        resources:
          requests:
            memory: "32Mi"
            cpu: "20m"
          limits:
            memory: "64Mi"
            cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: monitoring-frontend
  namespace: apps
  annotations:
    metallb.universe.tf/address-pool: metallb
spec:
  type: LoadBalancer
  selector:
    app: monitoring-frontend
  ports:
  - port: 80
    targetPort: 80
